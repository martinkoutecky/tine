bundle: B012-kimi
commit: f2f031bd80cdb9800f93b4672ab36ad6faa181a3
invariants: v1.2
date: 2026-09-01

=== FILE: crates/tine-core/src/oplog/operational_coordinator.rs (3182 lines)
purpose: Managed-storage (clean baseline-plus-manifest) operational coordinator: admits local semantic mutations, ingests provider batches, and reconciles external edits through one sealed pipeline — workspace-authority/binding proofs → plan → draft → capture → finalize → immutable manifest publication (the only irreversible boundary) → SQLite drain → Markdown projection drain — retaining an affine `CleanPublishedContinuation` across every post-commit failure for idempotent retry.
read: FULLY (production lines 1–1557; test region lines 1558–3182 not judged; a handful of production-region items are themselves #[cfg(test)]-gated, e.g. 32–76, 123–134, 556–575, 958–986)

I-1: PASS — the production region contains no raw filesystem writes; every durable mutation is sequenced through named boundaries: immutable manifest publication (`engine.commit_clean_prepared`, 468/669/897, "commit-last boundary" named at 613–616), the SQLite advance (`database.apply_engine_owned_accepted`, 1245, phase `SqliteDrain`), the manifested Markdown projection step (`replay_projection_turn`, 1362), the durable projection-turn journal append/checkpoint (1347, 1376), the durable sweep-membership append (881–892, "durable sweep membership append" named at 890), and the "durable foreground journal" named at 268–271; the byte-level protocol (fsync/rename/transaction) lives in the callee modules, which is collation's mapping to make.

I-2: CANNOT-DETERMINE — need the out-of-file recovery side: after a REAL process death between manifest commit (468/669/897) and drain completion, the in-memory `CleanPublishedContinuation` (378–393, holds a `PublishedHandoffLatch`) is gone, and nothing in this file rediscovers a committed-but-undrained batch or its pending projection-turn journal entries; candidates seen only from tests/callers are `crate::oplog::recover_incomplete_projections` (test import, 1574) and the runtime reopen path in local_active/sync_runtime. In-file the durable transitions are: prepare/bindings → publication (only irreversible commit) → SQLite apply → projection drain; same-process gaps have defined answers — pre-commit faults leave no durable effect and allow a fresh retry, post-commit faults retain an affine `DurablePending` continuation retried via `retry_clean_local_with_turns` (577–611), and the SQLite frontier before/after ambiguity is disambiguated at 1239–1256 — all exercised by the per-faultpoint matrix in the in-file test module (which per sweep rules is not itself proof-in-file).

I-3: PASS — multi-page intent reaches storage as one operation: one `OperationTransaction` (possibly many `SemanticOperation`s across pages, plus `page_home_hints` in the compound variant, 272–313) becomes one `PreparedBatch`/one manifest commit via `commit_clean_prepared`, and a two-path external rename (`requested_paths: &[&str]`, 736–742) likewise yields exactly one batch with one commit; no per-page save choreography exists in this file.

I-4: PASS — no production statement in this file constructs graph-file bytes; every byte decision is delegated (engine `draft/capture/finalize` calls and `super::projection::replay_projection_turn`, 1362), so the Logseq-readability contract cannot be violated by code here; round-trip byte assertions in the in-file test module exist but are not citable as production proof.

I-5: PASS — no logging, telemetry, network payload, or persisted diagnostic artifact is produced in the production region; failures are returned as typed `OperationalCoordinatorError` values (which embed callee error display strings — possibly including paths — whose downstream logging is a consumer-side, collation question).

I-6: PASS — no storage-authority derivation: the file is unconditionally the managed clean-runtime coordinator and operates only on the `CleanRuntimeSession`'s already-promoted admission/engine/SQLite parts; the only branches are pre-existing value dispatch (`LocalDraftSource::Promoted` vs test-only `Raw`, 1091–1140) and `BatchOrigin` identity assertions (831–838, 1173–1183), not backend decisions.

I-7: PASS — no format-version, legacy, or generation branch exists anywhere in the file (only `BatchInspection::{Ready,Staged,Absent}` state discrimination, 1292–1300); current user content committed into private archive objects is projected back to graph files by the same sealed pipeline before completion (`resume_clean_published`, 1200–1383), and retained manifest/oplog history is the v1.2-accepted private-only carve-out.

I-8: VIOLATION — operational_coordinator.rs:1252 — the SQLite-drain re-verification fails closed on "clean SQLite frontier is neither before nor after the durable operation" naming no in-scope failure scenario (crash recovery anomaly? honest concurrent instance? torn write?) — only the mechanism.
  - operational_coordinator.rs:489 — "clean manifest commit did not leave one accepted operation" fails closed after a successful commit call naming no scenario; near-identical at 692 (provider) and 920 (external).
  - operational_coordinator.rs:1445 — `verify_projection_bindings` retained-blocks "graph, engine endpoint, or receipt namespace binding mismatch" with no named scenario (wrong-state landing after a concurrent honest instance or graph switch is implicit only).
  - operational_coordinator.rs:1295 — "clean provider continuation batch is incomplete" fails closed naming no scenario (torn/partial archive read after a crash is implicit only); reprove_workspace_authority refusals (450, 845, 1240, 1356) do carry a mechanism comment (197–211) but still name no I-8 threat class.
  - Credit: 426–430 refuses with "requires external reconciliation before publication", which does name the external-editor scenario class.

I-9: VIOLATION — operational_coordinator.rs:472 — the manifest-commit failure at the only irreversible boundary is flattened to `error.to_string()` (468–472), discarding the typed failure family at the point a durable retry decision depends on it; the typed `OperationalPhase` survives, which is exactly the specimen's "names the phase, not what failed" shape.
  - operational_coordinator.rs:1248 — SQLite-apply error (`apply_engine_owned_accepted`, the drain where callers say Android durability-barrier defects surface) is display-flattened.
  - operational_coordinator.rs:1373 — projection-drain error (`replay_projection_turn`) is display-flattened.
  - operational_coordinator.rs:648 — identity-preflight typed error display-flattened; in total 27 `error.to_string()` sites plus 8 `format!("…{error}")` sites in the production region (e.g. 297, 465, 890) reduce every callee's typed error family to free text, though `phase`/`retained_block`/`revocation` stay typed.

I-10: CANNOT-DETERMINE — the file creates durable/blocking state: a retained `DurablePending` continuation whose published handoff latch keeps the graph's managed-text writer closed even when the continuation is DROPPED (in-file tests 2453–2471 and 2814–2835 assert exactly that), and terminally `retained_block`-marked continuations (`RetainedBlockReason`, 93–97). In-file retire paths exist for the live object (`retry_clean_local_with_turns` completes and calls `guard.complete()`; pre-publication failures call `cancel_prepublication`); but what retires an abandoned latch after process death, and where repeated identical failures deduplicate/escalate, lives in the caller (sync_runtime holds `self.pending` and escalates to `DurableStuck`) — collate against that side.

I-11: VIOLATION — operational_coordinator.rs:207 — the `reprove_workspace_authority` doc claims "a few per external reconciliation, and none on the keystroke path", but `execute_clean_local` (the local-edit/keystroke path) performs workspace-authority reproofs at 230 (WindowAuthorization via authorize_coordinator), 450 (Publication), 1240 (SqliteDrain) and 1356 (ProjectionDrain, per turn) — the claim is contradicted by this file's own call sites.
  - operational_coordinator.rs:200 — the same doc block claims the five call sites are "immutable publication, accepted-history archive staging, tail admission, the SQLite advance, and each manifested Markdown projection step"; the actual five sites are WindowAuthorization×1, Publication×2 (450, 845 — listed once), SqliteDrain×1, ProjectionDrain×1 — `ArchiveStage`/`TailAdmission` (`WorkspaceAuthorityBoundary` variants, local_active.rs:415–418) are never reproved anywhere in this file, and the used WindowAuthorization boundary is unlisted.
  - operational_coordinator.rs:91 — `RetainedBlockReason` is documented as "Stable post-publication evidence that retrying the exact immutable batch cannot turn into progress", but it is constructed pre-publication at the Bindings phase (`classify_authorization_failure`, 257–261; `verify_projection_bindings`, 1445–1449) — contradicted by usage; nothing enforces "post-publication".
  - Claims recorded per the Ask: module doc (1–5, "crate-private coordinator … invoked for admitted local edits, provider ingress, recovery, and derived-state drains") — confirmed: `pub(crate)` visibility plus callers sync_runtime.rs (5534, 5642, 5692, 6046, 17327, 19986) and local_active.rs:1232; the 105–109 `continuation_required` retry-budget carve-out — unverifiable as live: its only constructor is `#[cfg(test)]`-gated and never called anywhere, though sync_runtime.rs:5715 reads the flag in production (see W); the 731–735 structural-matcher/lazy-genesis planning claim — unverifiable from this file (describes import.rs internals); the 613–616 "same commit-last boundary" claim — confirmed: all three entry points share the identical commit helper and continuation shape.

I-12: PASS — domain questions this file answers locally, in plain words: (1) workspace-authority still valid at a boundary (delegated to `admission.reprove_workspace_authority`, 212–220 — a delegate, not a second definition); (2) graph/endpoint/receipt namespace binding match (`verify_projection_bindings`, 1431–1452); (3) finalized batch kept its author/import identity (831–838, 1173–1183); (4) where the SQLite frontier sits relative to a durable operation (before/after/neither, 1236–1256); (5) which CRDT peer id an external import may claim (bounded probe, 1385–1429); (6) phase classification of a coordinator failure (`OperationalPhase`, `classify_authorization_failure`, `operational_fault_error`); (7) how many pages the graph has, by hand-pagination (`page_count_at_open`, 858–880) — a locally computed count whose canonical home may be SQLite; duplicates are collation's call, none declared as oracle/validator twins here.

I-13: PASS — one O(graph) site exists in-file: `page_count_at_open` (858–880) paginates the entire SQLite page inventory 512 rows per query; it is constructed on the external-reconcile publication path but handed to the recorder as a callback, and absence_sweep.rs:855–865 (read to resolve the trigger) invokes it only when an absence sweep actually opens, i.e. once per sweep around deletions rather than per keystroke — amortized, bounded whole-graph metadata, not per-keystroke work; all other in-file loops are bounded by constants (`CRDT_PEER_PROBE_BUDGET` = 8, 31/1392) or by the batch's own intents/turns.

I-14: CANNOT-DETERMINE — every operation committed here durably appends one immutable manifest + objects (468/669/897) and one accepted-history SQLite event (1245), and projection-turn journal entries (1347/1376) — state that grows with lifetime edit count — yet no compaction/rebaselining bound is visible in this file; the bound, if any, lives in the archive (`ObjectStore`), `SqliteFrontier`, and `hot_engine` replay modules. Reads performed here (`AcceptedBatchEvent::from_accepted`, 1210; `inspect_batch`, 1285) are per-batch lookups, but their cost vs. history depth is likewise not visible from here.

I-15: PASS — expensive primitives visible in-file, per entry point: `database.materialized_read()` twice per commit path (407+462 local, 803+852 external, 645-preflight+663 provider) — two full SQLite snapshot reads per mutation, one draft-time one commit-time, the only repetition I see whose justification (fresh post-latch snapshot) is not stated; `reprove_workspace_authority` twice per commit (authorize 1209/1059→230 + publication 450/845) and 1 + 1-per-projection-turn in resume (1240, 1356); `manifest().encode()` once, only on the test-only correlated path (435); the bounded peer probe at most 8 draft attempts on collision (1392–1422); exactly one publication (`commit_clean_prepared`) per operation and one archive lookup + one accepted-event rebuild per resume. Loops: the projection-turn `while` loop (1355) and the page-inventory pagination (858–880) — both noted under I-13/I-14.

I-16: N/A

I-17: N/A

I-18: PASS — durable artifacts this file causes to persist are keyed only by portable identities: graph-relative `ManagedPath`s, `BatchId`/`PageId`/`DocumentId`/device/session UUIDs, `ContentDigest`s and deterministic `CrdtPeerId::external_import_candidate`/`SessionId::for_external_import_author` (809–817, 1392–1405); the in-memory `PreparedSqliteIdentityTransition` rides only the continuation (381) and is re-derivable (sqlite.rs:4544–4547), so no machine-local path, absolute path, or run-local pointer is persisted by this file; field-level portable/device-local classification lives in the payload type definitions (manifest, sweep, receipt, journal), outside this file.

I-19: N/A

I-20: PASS — async/stale-result guards are pervasive: workspace authority is re-proved immediately before publication (450, 845), before the SQLite advance (1240) and before every projection turn (1356); the SQLite apply lands only after an explicit frontier-root equals-prior/equals-post binding check (1239–1256, "neither before nor after" poisons); the prepared batch's identity is re-asserted against draft values after finalize (831–838, 1173–1183); graph/endpoint/receipt bindings are verified at preparation start (1066) and re-verified on the publisher guard (1079–1083); continuity between draft-time and commit-time state is the `commit_claim_source`/identity preflight pair (433, 462–468).

I-21: CANNOT-DETERMINE — acquisitions and their releases in-file: claim sources dropped on every path (432, 487, 690, 818, 918, RAII elsewhere); minted handoff cancelled on Noop/Blocked (776–781) and RAII-dropped before publication; published latch released via `published.cancel_prepublication()` on every pre-publication failure (474, 677, 887, 905) and via `guard.complete()` on success (517, 603, 721, 948) — BUT on the abandonment path (continuation dropped, retried never), in-file tests 2453–2471/2814–2835 assert the latch is deliberately NOT released and the managed writer stays blocked; who reclaims that latch after process death or graph switch is not answerable from this file — need the runtime/graph-drop recovery side.

I-22: CANNOT-DETERMINE — two untrusted inputs enter here: (1) externally modified graph file paths and their Markdown/Org bytes, accepted at `execute_clean_external` (736–742) and flowing through `plan_clean_affected_import` (772) into `capture_external_author_transaction` (820–824) — the path-escape normalization and any size/nesting bounds live in `super::import`/engine code not visible here (the "../escape.md" refusal exists only as a test, 2587–2594, not citable as production proof under sweep rules); (2) provider-transport `PreparedBatch` bytes at `ingest_clean_prepared` (617–729) whose manifest/object authentication happens inside `engine.commit_clean_prepared`, also not visible here.

W:
- operational_coordinator.rs:1468–1556 — the fault-injection harness is compiled into production: the pub(crate) `OperationalFaultPoint` enum, the `OPERATIONAL_FAULT`/`OPERATIONAL_ACTION` thread-locals and all 16 production-region `fault()` call sites are ungated no-ops in release builds (the only fault setters are `#[cfg(test)]`), and `OPERATIONAL_ACTION` has no setter anywhere in the repository at all (grep: only its declaration 1475 and the read/take 1505–1513) — an unreachable code arm permanently on the mutation path; asymmetrically, `ingest_clean_prepared` alone gates its `AfterManifest` fault behind `#[cfg(test)]` (706–710) while the twin sites at 503 and 934 do not.
- operational_coordinator.rs:123–134 — the only constructor that can set `continuation_required = true` is `#[cfg(test)]`-gated, private, and never called anywhere in the repository, yet production code (sync_runtime.rs:5715) branches on `is_continuation_required()` — a live production check against a state production can never construct, described at 105–109 as preventing a real wedging hazard.
- operational_coordinator.rs:999 — `PreparedLocalMutation.endpoint` is minted, bound-verified and then never read: both consumers destructure it away (`endpoint: _`, 443 and 1034).
- operational_coordinator.rs:577 — `retry_clean_local_with_turns` returning `CleanLocalMutationState` is in fact the retry path for clean EXTERNAL continuations as well (in-file tests 2272, 2502, 2720; sync_runtime.rs:5692); there is no external-named twin and nothing in the signature says so.
- operational_coordinator.rs:1394 — CRDT peer candidate 0 is silently skipped and only the exhaustion error (1423–1428) mentions "nonzero"; nothing states why 0 is an invalid peer id.

P:
- operational_coordinator.rs:465–524 — the publication→placeholder-continuation→`resume_clean_published`→fold-result epilogue is hand-copied four times (465–524, 661–728, 893–955, plus the retry disambiguation at 470–485/671–687/899–915) instead of one helper, and the copies have already drifted once (the `#[cfg(test)]` asymmetry at 706 vs 503/934).
- operational_coordinator.rs:294 — the literal "clean SQLite identity candidates are unavailable: {error}" is pasted at 7 sites (294, 344, 407, 462, 663, 803, 852) and "did not leave one accepted operation" at 3 (489, 692, 920) — copy-pasted failure vocabulary inviting exactly the drift I-9 exists to catch.
- operational_coordinator.rs:858 — `page_count_at_open` hand-rolls a 512-row cursor loop over the page inventory to compute a count a single SQLite aggregate query would answer.
- operational_coordinator.rs:1408 — `draft_with_bounded_peer_candidates` clones the whole `ImportExecutionMaterial` per probe attempt (1408/1410), up to 8×, although the attempts differ only in the candidate peer id.
- operational_coordinator.rs:1045 — `prepare_local_inner` carries `#[allow(clippy::too_many_arguments)]` with 9 parameters, four of them `Option`s — a parameter struct was the boring available thing.
- operational_coordinator.rs:449 — an internal flow invariant is enforced by `expect("clean local publication requires identity preflight")` (panic) while the same class of misuse three calls earlier gets a typed error ("identity-preflighted twice", 1016–1021) — pick one discipline.
