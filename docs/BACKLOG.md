# Tine — Backlog & Roadmap

The public front-door for not-yet-done Tine work: what's next, what's deferred, and what's
explicitly out of scope. (Detailed internal engineering/data-safety debt is tracked separately
and privately; **lsdoc**, the parser, has its own backlog in its own repo.)

**Categories are deliberate and distinct:**
- **In flight** — being built right now.
- **P1 / P2** — will do; P1 next, P2 when P1 clears.
- **Deferred** — genuinely might do later, but no slot yet. *Deferred is NOT WONTFIX.*
- **WONTFIX** — decided we are not doing it, with a reason.

Kept current in the same chunk of work: add items when they surface, remove them when done,
move them between categories in place.

---

## In flight (being built now)

_Empty. (The "Help improve Tine" lsdoc↔mldoc diff panel — [ADR 0018](adr/0018-in-app-lsdoc-mldoc-diff-panel.md) — shipped once the `parse_document_json` lsdoc-wasm export landed.)_

---

## P1 — do next (high value, bounded scope)

| Item | Notes |
|---|---|
| **Logbook / time-tracking** (Martin, Jul 5 2026) | Bring OG's clock/time-tracking to parity. **Researched → `subagent-tasks/notes/logbook-og-research.md`** (OG source cited throughout). **Format:** an org `:LOGBOOK:`/`:END:` drawer of `CLOCK: [start]--[end] =>  H:MM:SS` lines, timestamps `yyyy-MM-dd E HH:mm[:ss]` in local time, drawer placed after title/SCHEDULED/DEADLINE/properties (`util/clock.cljs`, `util/drawer.cljs`). **Behavior:** clocks are a *side-effect of marker cycling* (no start/stop button) — `handler/editor.cljs` `with-marker-time` clocks-in on →DOING/NOW, clocks-out on →TODO/LATER/DONE, gated by `:feature/enable-timetracking?` (default ON). A per-block elapsed badge + a Type/Start/End/Span tooltip show totals. **Tine already has** the drawer round-trip + hide (`doc.rs` keeps it, `block.ts visibleBody` hides it, lsdoc treats it as an opaque `Drawer`) and OG-parity marker cycling (`src/editor/marker.ts`); it's **missing** the clock side-effect, the summary/totals, and reading the config keys. **Effort ≈ 3–4 days** full parity (or ~1–1.5 for a marker-cycle-only MVP): a clock-in/out module in `tine-core` mirroring `drawer.cljs`+`clock.cljs`, a hook in `cycleMarkerSmart`/the save path, a summary that trusts the stored `=> span`, config keys, and a badge+tooltip UI. **Risks:** byte-exact timestamp round-trip (the `E` weekday + seconds-default), double clock-ins on a running clock (replicate OG's `not logbook-exists?` guards), timezone/DST (trust the stored span, don't recompute), and NOT adding a second drawer parser (one-parser rule). |
| **Recursively scan sub-directories under `pages/`** ([#21](https://github.com/martinkoutecky/tine/issues/21), Jul 5 2026) | Reporter (Org user, Linux Mint) archives pages into **real sub-folders** under `pages/` (e.g. `pages/client-a/…`); OG surfaces those nested pages in the Pages list and makes them searchable/linkable, but Tine shows only the top-level files. **Current state:** the page/journal scan is a **single-level `fs::read_dir`** (`model.rs` `list_pages` ≈ ln 755, memoized ln 330) — it never descends — and the path model is **flat by construction**: `resolve_rel` (`model.rs:528`), the security gate for every path-addressed read/save, accepts **exactly one segment** under `journals/`/`pages/` and rejects any nested subdir as traversal. So this isn't a one-line `WalkDir` swap — the real work is **widening the page-identity/path model to hold a sub-path** while keeping the "can't escape the graph" invariant, then threading it through the cache key, `rel_path`/`resolve_rel`, page-name ↔ file mapping, and the sidebar Pages list. **OG-parity checks BEFORE building:** (1) how OG maps a nested file to a page name — does `pages/client-a/foo.md` become page `foo`, `client-a/foo`, or the namespace `client-a/foo` (`/` = OG namespace separator, which also drives namespace pages)? This intersects Tine's existing `___`/`%2F` namespace-filename encoding ([[tine-namespace-encoding]]) — a real folder vs an encoded-`/` filename may collide. (2) Where new pages get **written** when their name implies a folder. (3) Whether journals recurse too (likely no). **Effort:** unmeasured; model change + scan + write-path, gated on the OG-mapping answer. Don't ship the scan without resolving the name-collision question. |
| **In-page find (Ctrl+F) on normal pages** (Martin, Jul 5 2026) | **Confirmed missing.** `Ctrl+F` is bound **only inside the PDF viewer** (`PdfViewer.tsx:557`, scoped to the PDF text layer); there is **no `mod+f` binding** in the central `keybindings.ts`, and Tauri/WebKitGTK ships **no default browser find-in-page UI** — so pressing Ctrl+F on an ordinary page does nothing. Distinct from what *does* exist: **Ctrl+K** (QuickSwitcher, `keybindings.ts:89`) is a graph-wide grouped search incl. full-text **Blocks** — that finds across the graph but is not a browser-style *find-and-highlight-on-the-current-page*. **Work:** a find bar (input + next/prev + match count + highlight) driven in the WebView. Caveats: matches inside **collapsed/unmounted** blocks (block-body lazy render, ADR 0008) and folded content won't be found by a naive DOM `window.find`/text-walk — needs to expand/mount hits or search the doc model; interacts with the caret/focus cluster and any future DOM windowing (which lists find-in-page as a risk). **OG-parity check:** confirm OG's own in-page find affordance (Electron `findInPage`?) before matching its exact UX. |

---

## P2 — backlog (real, lower urgency)

| Item | Notes |
|---|---|
| **Rendered-copy fidelity — still open (math + residual gaps)** | The Jul 3 2026 pass (`f1c5721`) made *Copy / export → Rendered* resolve block refs + user macros. **Still open:** (1) **Math renders as TeX, not typeset/text** — `$…$`/`$$…$$` copy the TeX source. A faithful plain-text form is genuinely lossy (a typeset fraction/integral has none), so this is a per-construct call: keep TeX for display math, but maybe a Unicode approximation or KaTeX text-content for trivial inline math (`E=mc²`). Martin wants this tracked as open, not closed. (2) **Best-effort ref cache** — a `((uuid))` only resolves if it was already rendered on screen in this graph epoch (`resolvedBlockRefSync`); exporting an off-screen subtree, or a ref never rendered, falls back to the bare uuid. Making the export path *await* the async resolve (instead of a sync cache read) would close it. (3) **Built-in/provider macros stay literal** in copy (`{{video}}`/`{{embed}}`/`{{query}}` …) since they're interactive widgets on screen; some have a sensible text form (`{{video url}}` → the url) if wanted. (4) A block ref copies only the referenced block's **first visible line** (matches on-screen `BlockRefView`); resolving a multi-line/block-level target more fully is open. **→ Plan: [docs/plans/rendered-copy-fidelity.md](plans/rendered-copy-fidelity.md).** |
| **Plugin CSS-variable alias shim** | Alias OG `--ls-*` CSS variables so the Awesome-Styler theme family "mostly works". A themes-compat slice, **not** full plugin support (that's WONTFIX). ~1–2 days. **→ Plan: [docs/plans/plugin-css-var-shim.md](plans/plugin-css-var-shim.md).** |
| **Datalog query coverage expansion** | A scoped subset of advanced Datalog queries works today (unsupported clauses are flagged, not silently dropped); expand coverage on demand. |
| **Quick-capture browser extension (Firefox)** | Snap a page/selection/link into today's journal or an inbox page via a capture template. Prefer a watched drop-file (no open port; rides the existing file-watch + journal-append). |
| **Query-engine indices + finer live-query refresh** (the sheets/supertags pre-req) | **Not needed today — verified Jul 4 2026**, recorded so the design is ready when it is. Current cadence: live `{{query}}` panels re-run at most ~1 Hz while typing (400 ms save debounce `persistence.ts:190` → `cache_gen` bump on save `model.rs:1488` → 700 ms `dataRev` debounce `persistence.ts:141` → panels keyed on `dataRev` re-fetch, `Macro.tsx:142`); each re-run is a native scan over `OnceLock`-cached block facets (`doc.rs:150`, O(1) field checks) ≈ ms per query at current graph size; and scoped eviction #52 (`model.rs:1504`) keeps unaffected memoized results — the keep/evict test is **two-sided** (evict if the page was in the result OR now matches the predicate, re-running the query's real `Pred::eval` against the post-edit doc, `query.rs:292`), fuzz-guarded by `tests/derived_cache_fuzz.rs`. **Becomes real when** query count × graph size × expressiveness grows: supertag/sheets dashboards with many live views, sort/group/join semantics, or much larger graphs. **Design shape:** (1) incremental facet indices (marker / priority / tag / page-ref → block ids) maintained in `cache_upsert` alongside the page cache, so query eval becomes index intersection instead of the `with_pages` full walk (`query.rs:221`); (2) per-query invalidation subscriptions replacing the global `dataRev`-keyed refetch, so an edit only re-renders the queries it actually evicted; (3) diff results before re-render — **DOM churn repainting big result lists ~1 Hz in WebKitGTK is the likely first *felt* cost, not the Rust scan**. **Alternative/complement (Martin's observation, Jul 4 2026): incremental result patching** — the eviction test already evaluates the edited page, so an evicted result could in principle be patched (`old − page's old group + page's new matches`, O(page) per edit) instead of fully recomputed (O(graph), `query.rs:221`). Deliberately NOT done today: global operators break local patching (`sample n` truncation `query.rs:270` — blocks cut by the limit are unknowable without a rescan; same for future sort/limit/group), and the current invariant "a cached result is always a full-recompute output" is what keeps the fuzz oracle trivial and keep/evict drift-free. If sheets adds sort/group/limit, prefer indices (fast recompute) over patching (correctness surface); patch only pure per-block-predicate queries if at all. Risks: index consistency with `cache_gen` + aliases + day-rollover (`today` in memo keys); extend the derived-cache differential fuzz oracle to cover the indices. **Watch signal:** Martin's sidebar TODO-query dashboard (workflow he dropped in OG, resumed Jul 2026) feeling sluggish while typing — first diagnose *which* lags: typing latency (save path; shouldn't happen) vs sidebar repaint stutter (DOM; cap result-list size, collapse coarse queries) vs stale results (harmless debounce). |
| **Block DOM windowing on very large pages** (measurement-gated) | Current state (ADR 0008): lazy render-once-**keep** — block bodies mount via IntersectionObserver-gated `AstBody` (~11/2000 render at load) but **never unmount**, so a long scroll session on a big page accumulates DOM; historically WebKitGTK layout/paint — not parsing — is what a big page *feels* like (per-block parse is µs + LRU-cached; see the scroll/render notes and the #12 jitter lesson: measure in the **real** WebKitGTK app, headless SW rendering does not reproduce compositor behavior). **Measure first:** a long-scroll probe on a synthetic huge page tracking DOM node count / memory / frame time, plus `npm run bench` (`scrollBig`). **Build only if measurement shows it:** true windowing = unmount block bodies scrolled out of the viewport, keeping height placeholders (preserve scroll geometry + anchor targets), or extend the `content-visibility` approach (mind the clipping gotcha from the earlier feed fix). Risks: the just-stabilized caret/focus cluster (ADR 0013 / editorController — never unmount the focused/edited block), block-ref + backlink anchor jumps into unmounted regions, find-in-page, e2e caret-harness assumptions. **Trigger:** a big page is sluggish AND the profile blames layout/paint rather than parse. **May also fix the 4K/1.5× fractional-scaling slowness** (row below): Martin (Jul 4 2026) reports *scrolling* is the worst symptom there, and windowing paints far fewer pixels per frame — so this is the leading candidate lever for that too, but still measurement-gated on Martin's box (headless uni server can't reproduce 4K/GPU). |
| **Performance: remaining levers** | The July 2026 whole-codebase audit (perf × data-safety × architecture, two independent auditors per dimension) closed the found eager/superlinear paths (graph-open foreground scans, bulk icon lookup, batch block-ref resolution, advanced-query memoization). Remaining: block **DOM windowing** (own row above) and flipping the CI bench job from advisory to gating once shared-runner noise is calibrated. Context for both: the Jul 4 2026 lsdoc third-party bench + Tine parse-cadence map — parsing is only user-visible at graph-open (background-warmed; a pre-warm search triggers a synchronous whole-graph build, ~1–2 s per 10 MB), never on keystrokes; the parser-side lever lives in **lsdoc's own backlog** ("plain-text fast path", expected ≈2–3×). |
| **UI sluggish on a 4K display at fractional scaling** (Martin, Jul 4 2026) | Fast on a native 1920×1200 laptop panel; slow on a 4K screen at **KDE Plasma 1.5× fractional scaling**. **Env workarounds tried and FAILED (Jul 4):** `GDK_SCALE=2 GDK_DPI_SCALE=0.5` → UI too small (effective 1.0×) *and still slow*; `TINE_GPU=0` (software paint) → *also too slow*. That two-way negative is diagnostic: integer 2× backing is slow too (so it's not specifically the fractional-downscale path), and software is slow too (so it's not a GPU/DMABUF-downscale pathology) → the cost is **raw device-pixel paint/composite throughput in WebKitGTK**, proportional to pixel area (1.5× on 4K ≈ 3–4× the 1920×1200 pixel count). No env flag fixes this; it's a paint-budget problem to shrink in *our* code. **Still worth an A/B (confirms + possible daily workaround):** `GDK_SCALE=1 GDK_DPI_SCALE=1.5` — draw at 1× device pixels (cheap), bump only font size to 1.5× (readable), at the cost of small-ish icons/padding; if *this* is fast, it nails the diagnosis. **Real levers (in our control), highest-value first:** (1) a **high-DPI "reduce visual effects" mode** — kill backdrop-filter/blur (modals, overlays), heavy box-shadows, and drop the per-`.query-block` `translateZ(0)` compositing layers ([[tine-rendering-perf]] pt 4; each is a GPU texture that costs ~2.25× RAM/fill at 1.5×) — auto-enable above some device-scale or expose a toggle; (2) land block **DOM windowing** (row above) — fewer painted nodes is the biggest win at 4K; (3) investigate rendering the webview *content* at a capped internal scale (<window scale) and letting the compositor upscale — trades crispness for fill-rate. **Need Martin's box to profile** (uni server is headless, no GPU); the first question is *what* is slow — scroll, typing, or steady-state — since it points at (2)/(1)/(1) respectively. |
| **Theming: semantic-token sweep → prebuilt themes** | Tine is already on ~30 semantic CSS tokens with working custom.css injection; a ½–1 d sweep of the ~140 remaining hardcoded colors (plus defining the referenced-but-never-set `--accent`, and moving theme persistence off localStorage, which doesn't survive restarts in WebKitGTK) makes prebuilt themes nearly free (~1 d for 2–3, incl. an OG-Classic port). Feeds directly into the existing `--ls-*` alias-shim item below. |
| **Slash-menu autocomplete UX** ([#15](https://github.com/martinkoutecky/tine/issues/15)) | Two OG-parity/discoverability gaps in the `/` autocomplete: (1) a **visible scroll affordance** — WebKitGTK hides scrollbars, so overflow items below the 280px fold look absent (affects every menu: slash, block-ref, page); (2) an **OG-style `/template` picker/submenu** — reporter expects Logseq's dedicated template chooser. Backend discovery + listing are verified working; this is the UX layer. Blocked on a repro for the "sees only one template" part. |
| **ARM builds — validate the artifacts on real hardware** (enabled Jul 5 2026) | **DONE (build side):** `release.yml` now has Windows-aarch64 (cross-compiled) + Linux-aarch64 (native `ubuntu-22.04-arm` runner) matrix entries — they build starting with the next tagged release. **Remaining:** nobody has run the produced binaries on real ARM hardware. On the first ARM release: (1) confirm the Windows-ARM cross-build actually links (ARM64 MSVC tools on the runner) and the NSIS/portable artifacts appear; (2) confirm the Linux-ARM `.deb` builds; (3) get a Surface Pro X / Snapdragon and an Asahi/Pi tester to confirm the apps *launch and run*. If the first tagged build fails on either ARM target, the fix is here (may need an explicit ARM64 MSVC component install for Windows). Historical context on the wrinkles handled: Windows `--target` outputs under `target/<triple>/release/` (threaded via `win-exe-dir`); Linux ARM ships `.deb`+raw-binary, AppImage-on-ARM skipped. |

---

## Deferred — genuinely later, no slot yet (NOT WONTFIX)

| Item | Notes |
|---|---|
| **Android app (Tauri v2 mobile)** | **Under evaluation — promising.** A focused ~1–2 week project, not "free" but not blocked: Logseq's own Android app proves the "operate directly on a synced folder of files" model works (via all-files storage access + real filesystem paths), which is exactly Tine's file model — so the Rust core ports almost unchanged, and Android's Chromium WebView is an *upgrade* over Linux's WebKitGTK. Main costs: a small Android folder-picker plugin, a mobile UI/UX polish pass, and gating the desktop-only bits. Cheapest first step: `tauri android init` + run the existing UI in the emulator. |
| **Op-log sync layer above Syncthing (outl-style CRDT)** | Make multi-device sync conflict-free *by construction*: each device appends only to its own `ops-<actor>.jsonl` op log (Kleppmann 2022 move-op tree CRDT + clock-ordered merge), Syncthing/Dropbox just moves the files — one writer per file means the transport can never create a conflict; `.md` files become regenerable projections, so any conflict copy of a projection is discardable. External/plain-editor edits are folded back into the log via block-identity matching (Tine advantage: OG's `id::` gives *exact* identity where outl is heuristic). Validated design exists in the wild: outl (github.com/avelino/outl, MIT) — study at `subagent-tasks/notes/outl-study.md`; **port the ideas, not the code** (their reconcile layer is welded to their own non-Logseq dialect; only `outl-core` ~2k lines is clean). Why deferred: the CRDT core is the easy 20% — the projection/reconcile engine is the most data-dangerous subsystem Tine could grow (heuristic identity rewrites the real graph) and a multi-week track that would displace everything. Prereq if ever started: the P2 conflict-file merge item first, plus a standalone-prototype phase with a differential fuzz oracle before it ever touches a real graph. |
| **Recent-graphs list / multi-graph switcher** | The sidebar graph control (shipped Jul 5 2026) makes *opening/creating* a graph discoverable, but there's no **persisted list of graphs** to one-click between (OG's dropdown lists every added graph). Adding it means persisting a `recent graphs` list (path + display name) through the Rust backend — [[tine-localstorage-ephemeral]]: WebKit localStorage doesn't survive a restart, so it must round-trip to a file — and rendering the entries in the existing switcher menu (switch = `switchGraph`-style `loadGraphPath`). Also fold in: forwarding a `tine /other/graph` argv to an **already-running** instance to switch (today the positional arg only resolves at cold start via `resolve_root`; the single-instance handler only special-cases `--capture`). Deferred: real value but not urgent; the open/new actions cover the stated need ("choose other graphs in the App / command line"). |
| **Graph view** | The visual node-link graph of page links. Martin's Jul 5 2026 "graphs" vote was almost certainly *this* (not multi-graph switching). **Researched** → `subagent-tasks/notes/graph-view-research.md`. Findings: the **data is nearly free** — each block's memoized `BlockProjection` (`doc.rs`) already carries `refs_page`/`refs_norm`/`block_refs`/`backlinks`, and namespaces derive from `/` in page names, so the backend is **one edge-list builder command (~1 day)**, a variant of the existing `referenced_page_names` scan (NB: `src/graph.ts` is graph-*folder* management, unrelated). OG builds `{nodes,links}` off DataScript (page-ref+tag+namespace edges, node size ∝ `cbrt(ref_count)`) and renders a live main-thread d3-force sim → WebGL (`pixi-graph-fork`). **Tine should diverge on perf** (WebKitGTK prime directive): d3-force in a **Web Worker with a convergence stop**, render **Canvas 2D first** (WebGL only if a spike proves it needed, never SVG at scale), **default to the bounded local/neighborhood graph** with a node cap on any global view. **Effort (unmeasured):** ~3–4 days for a separable **local-neighborhood MVP** (the recommended first bite — de-risks the renderer), ~1.5–2 weeks for full OG-parity global+local. **Top risk:** WebKitGTK render perf on Martin's real large graph is unmeasured → wants a rendering spike before committing a renderer. Feeds the "Interactive graph view in HTML export" row below as a near-free follow-on off the same builder (same node-cap caveat — ships to the open web). |
| **Interactive graph view in HTML export** | The static-site export has sidebar + search but no interactive graph yet. |
| **macOS notarization** | The macOS build is unsigned, so Mac users hit the "unidentified developer" wall (right-click→Open works around it). Signing needs an Apple Developer ID ($99/yr) — exploring a shared/borrowed ID or revisiting later. **Deferred, explicitly not WONTFIX.** |
| **Verso/Servo engine swap** | A longer-term answer to WebKitGTK's scroll/render gaps. Servo's web-compat for a dense editor isn't ready yet; revisit ~early 2027. |
| **Nested-grid ("breadth") views** | A TreeSheets-inspired grid over child blocks — a post-1.0 exploration. |
| **Live-preview editor (CodeMirror 6)** | The only honest route to Obsidian-style in-text liveness (widget decorations, marker hiding, real bold while typing): ONE CM6 instance for the single edited block, decorations fed from lsdoc's AST + inline spans — no second parser, ~76–116 KB gz. **This is what Martin's "live facet chip" idea actually needs** (the marker shows as raw text while the caret is in/adjacent to it, and becomes a clickable chip once the caret leaves = CM6 widget-decoration + reveal-source-under-cursor). **The old blocker is cleared:** lsdoc now ships inline spans (v0.4.0). Remaining cost: multi-week, and it reopens the just-stabilized caret/focus cluster (ADR 0013, editorController, the e2e caret harness is textarea-hard-wired). A cheaper narrowing — a scoped CM6 spike on JUST the task marker — still pays most of the CM6 *integration* cost (the expensive part is adopting CM6 + caret parity, not the per-construct decoration). Decision pending Martin. The transparent-textarea overlay is NOT an alternative for chips (see WONTFIX — glyph metrics must stay byte-identical). |
| **PDF export — polish follow-ups** | Whole-page **Export to PDF…** shipped Jul 5 2026 (ADR 0021: reuse the HTML export + the webview's own print engine via a hidden iframe + `window.print()`). Remaining nice-to-haves, none urgent: (1) **verify the print dialog on real WebKitGTK** — the print *document* render is screenshot-verified (`scripts/shot-printdoc.mjs`), but nobody has confirmed the GTK print→"Save as PDF" flow end-to-end in the running app (the plan's 5-min smoke test; `window.print()` is documented to work on WebKitGTK, unlike the confirm/alert no-ops — [[tine-webkitgtk-confirm]]); (2) **block-subtree export** — currently exports the whole page; a "export this block ⌄" from a bullet's zoom/context menu would match the original ask; (3) **silent no-dialog export** (Route A: a Rust `export_pdf` command rendering into a hidden WebviewWindow + `webkit2gtk::PrintOperation::print()` with file-output `PrintSettings`) for a "choose filename, done" UX — Linux-easy, but Windows `PrintToPdfAsync`/mac `createPDF` are the per-platform cost; (4) **pagination fidelity** on very long KaTeX/wide-code pages is unmeasured. Research context: `subagent-tasks/notes/pdf-export-research.md`. |
| **Donations / sponsor link** | A single low-friction Ko-fi/Liberapay link + `FUNDING.yml`. Parked — low priority. |
| **Feature video clips for the website** | Looping clips of the motion features (PDF, audio waveform, video, tabs). Parked — headless capture is too heavy for now. |

---

## WONTFIX — not doing (with reason)

### By-design non-goals
| Item | Reason |
|---|---|
| **Whiteboards** | A separate application domain; Tine is a fast local-first outliner. |
| **Flashcards / SRS** | Needs a dedicated spaced-repetition review engine; out of scope for an outliner. |
| **Full plugin system (`@logseq/libs`)** | Months of work (a datascript engine + Logseq's render model); Tine coexists with Logseq instead. |
| **Built-in git** | Delegate to your own sync tool (Syncthing, etc.). |

### Subsystem / feature calls
| Item | Reason |
|---|---|
| **`function` macro** | Needs a Clojure (SCI) evaluator plus query-result plumbing. Large, rarely used. |
| **`cloze` / `cards` macros** | Only meaningful inside the SRS review loop → same reason as flashcards. |
| **Zotero connector macros** | Niche; needs Zotero data-dir + item-metadata resolution Tine doesn't have. |
| **CEF / Chromium engine swap (desktop)** | Evaluated and ruled out — too heavy (a multi-process Chromium bundle per OS + a JS-compat shim). Staying on WebKitGTK; Verso is the live long-term bet (Deferred). |
| **YouTube-timestamp seeking** | OG-faithful seeking needs the YouTube player API; not worth it. (A plain clickable timestamp is the most that would be considered.) |
| **Overlay/backdrop "live highlight" editor mirror** | The transparent-textarea-over-rendered-mirror trick can only honestly deliver metric-safe color changes — no chips, no marker hiding, no bold (glyph metrics must stay byte-identical to the textarea) — in exchange for a permanent font-metric-sync liability on WebKitGTK. The CodeMirror 6 route (Deferred) strictly dominates it. |
