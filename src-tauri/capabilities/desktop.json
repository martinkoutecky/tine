{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions (self-update). The updater plugin is compiled only on desktop (see Cargo.toml target deps), so its permission must not apply to mobile.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
