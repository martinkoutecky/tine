- Notes about [[parameterized complexity]].
	- n-fold IP shows up again — see [[n-fold IP]].
	- Key idea:
		- decompose the constraint matrix into blocks
		- solve via dynamic programming over the bricks
- LATER Read the new #SODA submission
- A code block:
  ```rust
  fn main() {
      println!("hello");
  }
  ```
